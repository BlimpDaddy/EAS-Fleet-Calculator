# EAS Fleet Calculator V1 — site mirror

Byte-for-byte mirror of https://eas-calc.pages.dev/ (downloaded 2026-07-23).

Run locally: double-click `START CALCULATOR V1.bat` (opens http://localhost:5179/).

NOTE: this is the site's BUILT output — `main.js` is the minified production bundle.
The original readable source only exists on its author's machine; a zip of that project
folder is still the thing to ask for. `style.css` however IS hand-authored source.
`serve.mjs`, the .bat and this README are local additions, not part of the deployed site.
